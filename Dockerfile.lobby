FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml README.md ./
COPY src/ ./src/

RUN pip install --no-cache-dir .

ENV PYTHONPATH=/app/src

EXPOSE 50002

CMD ["uvicorn", "distributed_smb.network.lobby_server_main:app", \
     "--host", "0.0.0.0", "--port", "50002"]
